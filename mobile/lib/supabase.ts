// Supabase client — real implementation lands in the backend wiring session.
// Placeholder export keeps imports resolvable while the rest of the app is scaffolded.

export const supabase = {
  isPlaceholder: true as const,
};
